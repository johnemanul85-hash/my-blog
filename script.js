const posts = [{id: 1, title: "Getting Started with Web Development", date: "March 20, 2026", category: "Web Development", excerpt: "Learn the basics of HTML, CSS, and JavaScript", content: "Complete guide to web development"}, {id: 2, title: "10 Tips for Better Code", date: "March 18, 2026", category: "Programming", excerpt: "Improve your coding skills", content: "Writing clean code is essential"}]; function loadPosts(postsToDisplay = posts) { const postsContainer = document.getElementById('postsContainer'); postsContainer.innerHTML = ''; if (postsToDisplay.length === 0) { postsContainer.innerHTML = '<p>No posts found.</p>'; return; } postsToDisplay.forEach(post => { const postElement = document.createElement('article'); postElement.className = 'post-card'; postElement.innerHTML = `<h3>${post.title}</h3><p>${post.date}</p><p>${post.excerpt}</p>`; postsContainer.appendChild(postElement); }); } document.getElementById('searchInput').addEventListener('keyup', (e) => { const searchTerm = e.target.value.toLowerCase(); const filteredPosts = posts.filter(post => post.title.toLowerCase().includes(searchTerm) || post.excerpt.toLowerCase().includes(searchTerm)); loadPosts(filteredPosts); }); const themeToggle = document.getElementById('themeToggle'); themeToggle.addEventListener('click', () => { document.body.classList.toggle('dark-mode'); }); document.addEventListener('DOMContentLoaded', () => { loadPosts(); });